# Dev image — runs the real dev server with hot reload against a bind-mounted
# source tree (see docker-compose.yml's `dev` service). Not used for production.

FROM node:22-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

EXPOSE 3000
CMD ["npm", "run", "dev"]
